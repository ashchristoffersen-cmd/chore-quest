# Optional: enables sync across devices. Leave unset for local-only storage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Shared document id — every device using the same value shares one dataset.
VITE_SYNC_DOC_ID=family
